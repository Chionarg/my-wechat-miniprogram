<view class="material-page">
  <view class="notice">
    当前显示保存到本地的 TXT 文本副本。
    不会因为打开本页而调用 AI。
  </view>

  <block wx:if="{{ready}}">
    <view class="header-card">
      <text class="file-name">
        {{fileName}}
      </text>

      <text class="file-type">
        {{typeText}}
      </text>
    </view>

    <view class="content-card">
      <text class="section-title">
        AI 整理
      </text>

      <text wx:if="{{hasAi}}">
        AI 已整理。
        当前版本暂不直接重新整理已有结果。
      </text>

      <button
        wx:elif="{{aiAvailable}}"
        bindtap="onAnalyzeTxt"
        loading="{{aiSubmitting}}"
        disabled="{{aiSubmitting}}"
      >
        {{
          aiSubmitting
            ? 'AI 整理中…'
            : 'AI 整理'
        }}
      </button>

      <text wx:else>
        当前 TXT 暂不支持 AI 整理。
        第一版仅支持单 Chunk、非超长 TXT。
      </text>
    </view>

    <view class="content-card">
      <text class="section-title">
        原文
      </text>

      <text
        class="material-content"
        selectable
      >
        {{content}}
      </text>
    </view>

    <text class="footer">
      TXT 第一版保存的是读取后的文本副本，
      不是微信临时文件本身。
    </text>
  </block>

  <view
    class="error-card"
    wx:else
  >
    {{errorText || '无法读取资料'}}
  </view>
</view>